#JSGF V1.0;

/**
 * JSGF Grammar 
 */

grammar grammar;

public <syntax> = ( kitchen | bedroom | livingroom | bathroom | garage | hallway | all)
                   (on | off);
public <shutdown> = (shutdown);